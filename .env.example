# GitHub OAuth App credentials (Settings > Developer settings > OAuth Apps)
OAUTH_GITHUB_CLIENT_ID=
OAUTH_GITHUB_CLIENT_SECRET=

# Base URL of the deployed site, e.g. https://jiwaku.vercel.app
# Optional: if empty, the OAuth functions derive it from the request host.
OAUTH_BASE_URL=
